---
description: Strict workflow for "修复 xxx 并部署" requests
alwaysApply: true
---

# 修复并部署工作流

当用户说“修复 xxx 并部署”时，必须严格按以下顺序执行：

1. 修改相关代码文件。
2. 在终端运行 `pnpm run typecheck`。
   - 如果通过，继续下一步。
   - 如果失败，先修复类型错误，再重新检查，直到通过。
3. 运行 `git add . && git commit -m "fix: [根据改动自动生成简洁的中文描述]"`。
4. 运行 `git push origin main`。
   - 推送前会自动触发 pre-push 钩子，内含类型检查和构建。
   - 如果推送失败，必须告诉用户具体错误，并停止流程。
5. 推送成功后，回复用户：
   "已推送成功，服务器正在自动部署。约 1 分钟后可访问 https://lab.aliberg.cn/m 验证。"

# 约束

- 不要跳过类型检查。
- 不要在未完成 typecheck 的情况下提交或推送。
- 提交信息必须是简洁中文描述，并以 `fix:` 开头。
- 如果推送失败，停止，不要继续尝试隐藏错误。
