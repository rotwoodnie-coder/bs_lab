---
description: bs_exp_data 为系统唯一使用库且表结构不可改；V2 路径禁止旧库连接与旧表资源
alwaysApply: true
---

# `bs_exp_data` 唯一真源 + 表结构不可改 + V2 禁用旧库资源（硬性）

## 0. 库名与范围（产品裁定）

**`bs_exp_data` 是当前系统唯一使用的数据库**（`DATABASE_URL` 等默认连接应对齐该库）。  
**不可更改该库的表结构**：助手在常规任务中 **不得** 编写或引导在 `bs_exp_data` 上执行 **DDL**（`CREATE`/`ALTER`/`DROP` 表与索引、改约束等），也不得把「为跑通功能临时改库结构」当作解决方案。

与 `no-legacy-db-for-formal-dev.mdc`（禁止把原库当正式数据源）配合使用。

## 1. `bs_exp_data` 表结构：冻结含义

1. **禁止**为赶功能、联调或在 **`bs_exp_data`** 上直接执行 DDL，或未经产品/评审流程的结构性变更；**禁止**建议用户「连上 `bs_exp_data` 执行一条 ALTER」作为默认排障手段。
2. 若业务确需演进结构，须在 **独立库/分支流程** 中设计与验证，并以 **已评审合并的仓库迁移脚本** 等形式交付；**不在本规则下**将「改 `bs_exp_data` 结构」作为助手侧默认可执行动作。
3. **允许**：对 **`bs_exp_data` 只读** 查询以对照契约、排查数据问题（在说明中标明「只读、目标库 `bs_exp_data`」）。

## 2. V2 版本：禁止再使用旧库资源

1. **禁止**在 **V2 契约** 下的读写路径（如 `v2-*-repository`、`/v2/*` 路由、以 `data_*` 与 `database/migrations/bs_exp_data.sql` 及已合并迁移为准的 Next Route Handler）使用 **`getLegacyDbPool` / `runLegacyQuery` / `LEGACY_DATABASE_URL`** 或等价「换库名连 `bs_lab_data`」作为**默认数据来源**。
2. **禁止**为 V2 页面或 V2 API **新增**对旧表（如 `edu_*` 旧维表）的依赖；若仍存在历史桥接，须标注为**待删除的过渡代码**，不得扩展其职责。
3. **例外（仅排查）**：与 `no-legacy-db-for-formal-dev.mdc` 一致，仅限**只读对照、迁移分析**，且 PR/说明中注明「旧库、仅排查」，不得合并为 V2 正式路径。

## 自检清单

- 默认连接与文档中的「当前库」是否均指向 **`bs_exp_data`**？
- 本次改动是否会让 **V2 默认请求** 命中 **旧库连接池** 或旧表？
- 是否出现针对 **`bs_exp_data`** 的 **DDL** 建议或脚本（应拒绝或改为只读/走正式迁移流程）？
