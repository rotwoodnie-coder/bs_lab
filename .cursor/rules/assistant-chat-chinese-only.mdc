---
description: 强制助手默认使用中文对话
alwaysApply: true
---

# Cursor 对话语言强制规则（中文）

在本项目中，AI 助手（Cursor Agent / Chat）与用户的所有对话必须默认使用中文。本规则为全局强约束，除非用户明确要求英文，否则不得切换输出语言。

## 适用范围

- 聊天回复（分析、方案、确认、总结）
- 代码解释与变更说明
- 错误排查反馈、测试结果说明
- 向用户提问澄清需求

## 硬性要求

1. 默认使用简体中文回复，禁止输出纯英文回复。
2. 只有在用户明确要求“使用英文”时，才可临时切换语言；用户未明确要求时必须保持中文。
3. 代码标识符、路径、命令、协议名可保留英文，但解释文本必须为中文。
4. 提问、确认、风险提示、下一步建议必须使用中文。
5. 若历史上下文出现多语言混杂，新的回复仍以中文为准并保持一致风格。
6. 所有中文文案必须使用简体中文，禁止使用繁体字（如“請/實驗/說明/關閉”等）。
7. 若检测到繁体文本，必须立即改写为简体后再输出，不得直接沿用。

## 回复风格建议

- 简洁、结构化、可执行
- 优先说明“做了什么 / 为什么 / 下一步”
- 避免冗长客套

## 提交前自动扫描繁体字（强制）

为避免繁体字漏网，所有涉及中文文案的改动在提交前必须执行一次“繁体字扫描”。

### 扫描方式

- 优先使用 `rg` 对本次改动文件或目标目录扫描常见繁体字。
- 若命中结果，必须先改为简体，再进入提交或合并流程。

### 常见繁体字黑名单（基础集）

`請 實 說 點 擊 關 閉 開 啟 資 料 圖 示 範 圍 錄 入 步 驟 選 擇 頁 麼 為 與 會 後 臺 審 核`

### 推荐扫描命令（示例）

```bash
rg "請|實|說|點|擊|關|閉|開|啟|資|料|圖|示|範|圍|錄|入|步|驟|選|擇|頁|麼|為|與|會|後|臺|審|核" frontend packages/ui
```

### 通过标准

- 扫描结果为空，或命中项均已改为简体。
- 若某条命中为专有名词/引用原文，需在说明中注明保留原因。
