---
description: Frontend 交互页面仅用标准库（以 /debug/ui/all-components 为准）
globs: frontend/src/**/*.{ts,tsx}
alwaysApply: false
---

# Frontend 交互页面：标准库使用约定（强制）

## 唯一允许的 UI 来源

- 交互页面/业务组件 **只能使用** `@bs-lab/ui`（以及 `@bs-lab/ui/icons`、`@bs-lab/ui/react-table` 等该标准库的配套导出）。
- 标准库能力边界以 `http://localhost:4200/debug/ui/all-components` 展示为准；**不要**在页面里“临时”引入其它空间/实现来凑交互。

## 严禁事项

- **禁止**从 `packages/ui/src/**`、`frontend/src/components/ui/**` 或任意相对路径“直连”UI 原子组件实现。
- **禁止**在 frontend 直接 import 第三方 UI（如 Radix、lucide 等）；如需能力，必须由 `packages/ui` 封装并从 `@bs-lab/ui` 导出后再使用。

## 交互控件选型约定（默认）

- **单选**：优先使用 `RadioGroup` / `RadioGroupItem`。
- **多选**：使用 `Checkbox`（列表/树等场景可组合布局）。
- **下拉框**：使用 `Select`（需要“多选下拉”时，用 `Select` + `Checkbox` 组合实现，不引入其它组件库）。

## 自检清单（提交前必过）

- 页面内所有 UI 组件 import 均来自 `@bs-lab/ui`（或其官方子路径）。
- 若用到未在 `/debug/ui/all-components` 出现的组件/模式：先补齐标准库再回到页面使用。

