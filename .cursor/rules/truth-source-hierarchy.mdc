---
description: 真源权威链（设计意图→物理现实→呈现级）+ 冲突时 AI 停止询问机制
alwaysApply: true
---

# 真源权威链（Hard Constraint）

## 0. 权威定序（三层级）

当多个规则或文档对同一字段/实体描述不一致时，严格按以下优先级裁決：

### 最高级：设计意图（指名真源）

**`docs/iam/01-数据库最终开发前设计文档.md`**（下称《01 文档》）是所有持久化字段的**命名与语义真源**。

- 前端组件、API 契约（含 zod/OpenAPI）、TypeScript 中与持久化实体相关的语义字段名，必须与《01 文档》中对应表、字段的命名与拼写严格对齐。
- 禁止为「省事」使用缩写或口语化别名替代文档全名（例如文档为 `pic_url` 时，禁止在类型或 API 层长期用 `pic` 指同一列）。
- 《01 文档》未定义的字段、表或枚举：必须先向用户确认后再命名；禁止凭旧代码、Tab 补全私自造名。

### 执行级：物理现实（库表 DDL 与迁移）

**`database/migrations/bs_exp_data.sql`** 是当前系统**唯一使用库 `bs_exp_data`** 的 DDL 基线，配合**已合并编号增量迁移**（`00NN_*.sql`）构成物理真源。

- 前端 UI 展示的每个持久化字段，必须能在该 DDL 中找到对应的列定义，字段语义必须一致。
- 后端 Repository 层 `rowTo*` 函数必须覆盖对应表**所有列**（允许省略 `is_deleted` 等内部列，但不可省略 `create_user_id`、`create_time` 等可展示列）。
- 枚举值必须与 DDL 列值完全一致（DB 存 `"Teacher"` 则前端类型为 `"Teacher"`，不可是 `"Role_Teacher"`）。
- 新建/编辑表单：字段顺序与必填性以创建/更新 DTO 与表约束为准。
- **禁止**为赶功能在 `bs_exp_data` 上直接执行 DDL；必须通过已评审的编号迁移脚本演进。

### 呈现级：后端 DTO → 前端 ViewModel

- 后端 API 返回体中键名在语义上与库侧列名一致（传输层 **snake_case**）。
- 前端 `interface`/`type` 与表结构在字段集合与语义上应 **1:1 映射**（允许缺失只读列不展示，但不得在类型层用另一套平行字段冒充同一实体）。
- **禁止**在页面/组件里对 DTO 做解构时顺便改英文名作为业务字段别名（例如 `const { pic_url: pic } = row` 后全页用 `pic` 表示同一列）。
- **唯一例外——转换层（Transform/Adapter）**：仅在此层做不改变语义键的格式化（时间戳转 ISO、`y/n` 转 boolean）；转换后对象的主键名仍须与 camelCase 列语义一致。

---

## 1. 裁决规则（硬性）

若**物理 SQL（`bs_exp_data.sql`）**与**《01 文档》**对同一字段的命名/类型不一致，AI **必须停止编写代码**，并向用户提出以下格式的问题：

> 老聂，`xxx` 表的 `yyy` 字段在 SQL 中定义为 `aaa`，IAM 设计文档中写作 `bbb`，命名不一致。以哪个为准？

在获得用户明确答复前，不得自行拍板。

---

## 2. 命名映射（snake_case / camelCase）

| 层 | 命名风格 | 示例 | 映射规则 |
|---|---|---|---|
| DB 列 | snake_case | `create_user_id` | 真源，不可变 |
| 后端类型 | camelCase | `createUserId` | 严格 1:1 映射 |
| 前端类型 | camelCase | `createUserId` | 与后端一致 |
| API 请求体 | snake_case | `create_user_id` | 与 DB 列名一致 |

映射规则（严格一对一）：`user_id` ↔ `userId`、`pic_url` ↔ `picUrl`。禁止出现 `user_ID`、`userid`、`picURL` 等不一致形态。

重构当前文件时若发现字段名与真源链冲突，优先纠错（改代码与类型对齐真源），而不是保留错误拼写以兼容旧 Tab 习惯。

---

## 3. 向后端/API 对齐（UI 迁就库与契约）

- 展示层可做映射，语义层不可造假：允许在适配层/字典把 `y/n`、UUID 等转成文案或树节点；禁止在业务页长期用 mock 结构冒充与库不一致的实体。
- 标签与分组：下拉、筛选、表头文案优先来自 `/v2/dict/*` 或接口返回的 display 字段；库中仅存码值的，UI 必须明确是「码 + 字典解释」。
- 数据源必须来自目标开发库（`bs_exp_data`）/V2；本规则解决的是同一目标库上「谁迁就谁」：UI 迁就库与契约，而非库迁就 UI。

---

## 4. POST/PUT 请求体对齐

- 凡写入 MySQL 表某一列的请求体顶层 key，必须与对应 DDL 中的列标识符完全一致（snake_case）。
- 值写入 JSON 列（如 `comments`）时：使用与持久化 JSON 对象相同的 key 名；侧车子对象为 `_ref`（教材书目元数据）、`_ch`（章节扩展字段），内部字段一律 snake_case。
- 禁止在发往自研 API 的 JSON 里用 camelCase 表示对应表列的语义。
- 前端 UI 层仍可使用 camelCase 状态；在发起 `fetch` 前（通常在 `page.api.ts` 或 BFF 层）必须映射为 snake_case。

---

## 5. 自检清单（涉及实体字段或数据库变更时）

- [ ] 每个持久化字段能否在《01 文档》中找到命名依据？
- [ ] 每个持久化字段是否能在 `bs_exp_data.sql` 中找到对应列？
- [ ] 枚举值是否与 DB DDL 的列值一致（`"y"/"n"` 而不是 `"yes"/"no"`）？
- [ ] 后端 Repository 是否 SELECT 了所有业务列？
- [ ] 表单提交体中的 key 是否使用 snake_case 与 DB 列名一致？
- [ ] API 与 TS 是否分别为 snake_case / camelCase，且映射无歧义？
- [ ] UI 是否仅在 adapter/transform 做格式化，而未在页面层引入文档外的简称主字段？
