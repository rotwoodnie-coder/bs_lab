# 数据库建成后禁止 Mock（硬性）

当业务模块对应数据库表已经建立并可访问后，前端与中台页面必须切换为真实 API 数据源，禁止继续使用本地 mock/seed 数据作为运行时数据来源。

## 触发条件

- 已存在对应业务表（如 `edu_stages`、`edu_subjects`、`edu_grades` 等）。
- 或后端已提供可用接口并可联调。

## 硬性规则

1. 禁止在页面状态层、API 层继续保留 `mock -> api` 运行时分支（含环境变量开关保留 mock）。
2. 禁止在业务页面中读取本地静态种子数据（常量数组、localStorage seed、内存 mock store）作为主数据源。
3. 若真实接口失败，允许展示错误态与重试按钮；不允许自动回退到 mock 数据。
4. 新增页面必须默认对接真实 API，并明确 loading / error / empty 三态。
5. 测试数据仅允许存在于测试目录（单元测试、E2E fixtures），不得进入生产页面数据流。

## 执行清单

- 检查是否仍存在 `fetchMock`、`mockDb`、`seed` 等运行时代码。
- 检查 API 模块是否只有真实接口调用路径。
- 检查 hooks 是否在错误时仅提示并重试，而非回填 mock 数据。
- 检查页面是否处理 `loading` / `error` / `empty` 状态。

## 例外

- 仅在“后端接口未交付且用户明确要求临时 mock”时允许；一旦数据库和接口完成，必须在同一任务或后续首个联调任务中移除。
