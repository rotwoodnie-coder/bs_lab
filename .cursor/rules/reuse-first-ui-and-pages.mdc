---
description: 复用优先：同构页面与交互必须共享实现
globs: frontend/src/**/*.{ts,tsx}
---

# 复用优先（页面/交互）

## 核心原则

- 同构功能（查看/编辑/详情/管理）必须优先复用同一套组件骨架与状态逻辑，禁止复制粘贴出“新页面 + 新控件”并行维护。
- 任何新增实现前，先在现有页面、组件、hook、store 中检索可复用能力；可扩展则扩展，不可扩展才新增。
- 新增代码应以“提取公共组件 + 模式参数（如 view/edit）”为默认方案，而不是创建同类页面分支。

## 强制要求

- **查看与编辑同构场景**：必须复用同一个详情组件，通过只读/可写模式切换行为。
- **列表与详情联动场景**：字段定义必须单一来源，列表列、查看页、编辑页字段保持一致，禁止三处各自维护。
- **相似交互场景**（如原位编辑、批量操作、弹窗表单）：优先抽取通用交互组件或复用现有组件能力。
- **命名与入口**：同一业务域内避免出现语义重复页面（如 `xxx-view`、`xxx-detail`、`xxx-editor` 各自重建同构 UI）。

## 实施清单（每次改动必查）

- 是否先检索并评估了可复用实现（组件/hook/store）？
- 是否通过参数化（mode、readonly、variant）复用了现有组件？
- 是否避免创建同构新页面/新控件？
- 列表字段与查看/编辑字段是否完全对齐？
- 是否将新增通用能力沉淀到可复用位置，而非页面内私有重复实现？

## 例外条件

- 仅在交互模型或信息架构实质不同（非同构）时，允许新建页面/组件；
- 必须在变更说明中写明“为何不可复用现有实现”。
