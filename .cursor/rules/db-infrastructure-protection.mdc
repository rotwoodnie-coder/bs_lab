---
description: bs_exp_data 表结构冻结 + 禁止旧库与旧表作为正式数据源
globs: backend/src/**/*.{ts,tsx}
---

# 数据库基础设施保护规则（硬性）

## 0. 库名与范围

**`bs_exp_data` 是当前系统唯一使用的数据库**（`DATABASE_URL` 等默认连接应对齐该库）。  
本规则保护该库的结构完整性，并禁止在 V2 路径中使用旧库资源。

---

## 1. `bs_exp_data` 表结构：冻结含义

**不可更改该库的表结构**：助手在常规任务中**不得**编写或引导在 `bs_exp_data` 上执行 DDL（`CREATE`/`ALTER`/`DROP` 表与索引、改约束等），也不得把「为跑通功能临时改库结构」当作解决方案。

具体约束：

1. **禁止**为赶功能、联调在 `bs_exp_data` 上直接执行 DDL，或未经产品/评审流程的结构性变更；禁止建议用户「连上 `bs_exp_data` 执行一条 ALTER」作为默认排障手段。
2. 若业务确需演进结构，须在**独立库/分支流程**中设计与验证，并以**已评审合并的仓库迁移脚本**（`database/migrations/00NN_*.sql`）等形式交付。
3. **允许**：对 `bs_exp_data` **只读** 查询以对照契约、排查数据问题（在说明中标明"只读、目标库 `bs_exp_data`"）。

---

## 2. 禁止原库（旧库）作为正式数据源

### 2.1 硬性禁止

当前处于**正式开发阶段**时，**禁止**把「原有 / 旧版 / 迁移前」数据库（`bs_lab_data` 及 `edu_*` 旧表）作为应用、联调、验收或任何正式使用场景下的数据依赖来源：

1. **禁止**在本地/联调/预发等开发环境中，将后端或前端的默认连接、主业务读写指向原库。
2. **禁止**把原库中的数据当作新架构的权威真源驱动 V2 页面与 API 的常态化开发。
3. **禁止**编写依赖原库的长期脚本、定时任务、或「顺手查一下原库」固化进业务代码路径。
4. 新功能、数据模型与迁移验证必须以 `bs_exp_data` 为准。

### 2.2 V2 路径下的特殊约束

1. **禁止**在 V2 契约下的读写路径（如 `v2-*-repository`、`/v2/*` 路由、以 `data_*` 与 `bs_exp_data.sql` 及已合并迁移为准的 Next Route Handler）使用 `getLegacyDbPool` / `runLegacyQuery` / `LEGACY_DATABASE_URL` 或等价「换库名连 `bs_lab_data`」作为默认数据来源。
2. **禁止**为 V2 页面或 V2 API **新增**对旧表（如 `edu_*` 旧维表）的依赖；若仍存在历史桥接，须标注为"待删除的过渡代码"，不得扩展其职责。

### 2.3 允许的唯一例外：排查

仅限**问题排查、数据对照、迁移差异分析、事故调查**等明确需要时，对原库进行**只读**访问，且应：

- 在说明中注明"原库、仅排查"；
- 排查结束后不得将结论依赖继续绑定在原库上；
- 不得将原库只读结论写回为默认开发流程的一部分。

---

## 自检清单

- [ ] 默认连接与文档中的"当前库"是否均指向 `bs_exp_data`？
- [ ] 本次改动是否会让 V2 默认请求命中旧库连接池或旧表？
- [ ] 是否出现针对 `bs_exp_data` 的 DDL 建议或脚本（应拒绝或改为只读/走正式迁移流程）？
- [ ] 环境变量与连接串是否始终指向目标开发库，而非原库？
- [ ] 是否有代码、脚本或文档仍引导他人「连原库做日常开发」？
- [ ] PR 中若出现原库连接或大范围原库查询，必须说明"仅限排查"且不得合并为常态化路径。
