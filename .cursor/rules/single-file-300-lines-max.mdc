---
description: 采用500告警与300硬门禁的单文件治理，并在超限时拆分 hooks/types/components
alwaysApply: true
---

# 单文件 300 行硬约束与自动拆分规则

## 双阈值治理（长期生效）

- 开发告警阈值：`500` 行。用于提示“避免过度琐碎拆分”的工程节奏，不作为放宽硬约束的依据。
- 强制门禁阈值：`300` 行。仍为唯一硬性上线规则，提交实现必须满足。
- 当 `500` 与 `300` 出现冲突时，一律以 `300` 为准。

## 硬约束

- 严禁生成或修改后超过 `300` 行的单文件。
- 任何新增需求若会导致单文件超限，必须立即停止继续堆叠到该文件。
- 严禁新增或修改后超过 `60` 行的函数；超限时必须拆分为私有函数、hook 或工具函数。

## 分层执行区间（统一口径）

- `<= 220` 行：正常开发区，可继续迭代。
- `221 ~ 300` 行：预警区，优先进行“增量抽离”（先抽 hooks 或私有组件）。
- `301 ~ 500` 行：遗留过渡区，仅允许做减量重构，不允许新增复杂逻辑。
- `> 500` 行：高风险区，必须先输出拆分清单（File Manifest）并分阶段重构。

## 超限时的强制拆分结构

- `index.tsx`：容器入口，仅保留页面骨架、路由上下文、状态装配与组件编排。
- `[文件名].hooks.ts`：提取复杂 `useState`、`useEffect`、异步请求与副作用编排。
- `[文件名].types.ts`：集中存放 `interface`、`type`、枚举与对外契约。
- `_components/`：同级目录下拆分复杂子模块（如表格单元格、对话框内容、分区面板）。
- `hooks/`：当业务域复杂度持续增长时，按 Feature 建立目录化 hooks，避免单 hooks 文件再次膨胀。

## 主页面职责（原子化）

- 主页面文件仅保留页面骨架、布局与组件装配。
- 页面内禁止保留已抽离到 hooks/components 的重复实现。
- 业务逻辑应从“页面内联”迁移到 hooks 或子组件，避免上帝组件。

## 执行要求

- 修改前先评估目标文件行数与改动后体量；若有超限风险，先拆分再实现。
- 大文件重构任务必须先输出“拆分清单（File Manifest）”，待用户确认架构后分步骤实施。
- 对超过 `300` 行的现有文件，禁止全量重绘式改写；必须采用“增量注入”或“模块重构”模式，控制单次改动范围。
- 拆分应优先按业务块聚合（筛选区、列表区、详情区、弹窗区），避免无意义碎片化拆分。
