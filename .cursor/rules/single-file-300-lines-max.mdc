---
description: 职责优先、行数为辅的双重治理：函数单一职责、业务域拆分、可读性保护、300/500柔性门禁
alwaysApply: true
---

# 代码可读性与结构治理规则

## 1. 核心开发原则：职责胜于行数

### 1.1 单一职责函数（硬性）

- 单个函数必须控制在 **50 行** 以内。
- **严禁**在一个函数内处理两个独立的业务逻辑。例如：同一个函数里不应同时做"数据校验 + 数据库写入 + 日志记录"，应拆分为 `validate()` → `save()` → `log()`。
- 超过 50 行或含有双重语义的函数，必须拆分为私有函数、hook 或工具函数。

### 1.2 业务域拆分阈值（硬性）

- **严禁**在一个文件内混杂超过 **3 个子业务域**。
- 子业务域举例：在一个服务类里同时处理"权限验证"、"数据库写入"、"第三方消息推送"——这三个即达到 3 个域，应拆分为独立的 Repository、Service 或 Utility 文件。
- 判断方式：文件中的 `import`/函数/类如果涉及 3 个以上不直接相关的业务概念（如同时操作租户、课程、文件上传），即应触发拆分提议。
- 拆分提议应在当前任务结束前输出《拆分清单（File Manifest）》，待用户确认后实施。

## 2. 可读性"防缩水"条款

### 2.1 禁止压行（硬性）

- **严禁**为了缩减文件行数而合并变量声明、取消合理的逻辑换行或采用晦涩的简写。
- 每个独立变量独占一行声明；每个逻辑步骤独占一个代码块（允许合理的连续赋值）。
- 条件/循环体即使单行也禁止与 `if`/`for` 写在同一行。
- AI 在重构时不得将原本可读的代码压缩为难以追踪的单行链式调用。

### 2.2 注释强制性（硬性）

- 所有复杂的业务重算、跨表事务、非显而易见的边界处理必须保留 **中文结构化注释**，解释"为什么这么做"而非"做了什么"。
- AI 在重构时 **严禁删除** 原有的人工业务注释（包括"TODO""HACK""FIXME"等标记）。
- 注释不应是逐行的"翻译"，而应是逻辑闭环的说明。例如：

```typescript
// 注：先查学科再查年级，因为年级依赖于学科的 level_id 进行跨级过滤
const subject = await subjectRepo.findById(subjectId);
const grade = await gradeRepo.findByLevelId(subject.levelId);
```

### 2.3 强制繁体字扫描

提交前遵守 `scan-traditional-chinese.mdc` 执行繁体字扫描。

## 3. 柔性行数门槛（300/500）

行数门槛作为"健康检查工具"，而非唯一裁决依据。

### 3.1 300 行预警

当文件超过 **300 行** 时，AI 必须主动自检：**"当前文件中是否有函数违背了职责单一原则？是否有超过 3 个子业务域混杂？"** 若有，必须先输出拆分提议。

### 3.2 500 行熔断

**500 行是绝对上限**（纯类型/常量定义文件除外，但需在文件顶部注释说明"此为纯类型文件"）。达到此限制时，AI 必须停止向该文件写入新代码，先输出《重构拆分清单（File Manifest）》并征得用户同意后再实施拆分。

### 3.3 分层执行区间

| 行数区间 | 应对策略 |
|---|---|
| `<= 220` 行 | 正常开发区，可继续迭代 |
| `221 ~ 300` 行 | 预警区，优先进行"增量抽离"（先抽 hooks 或私有组件） |
| `301 ~ 500` 行 | 遗留过渡区，仅允许做减量重构，不允许新增复杂逻辑 |
| `> 500` 行 | 高风险区，必须先输出拆分清单再分阶段重构 |

## 4. 超限时的强制拆分结构

当行数或业务域触发拆分时，按以下结构组织：

- `index.tsx`：容器入口，仅保留页面骨架、路由上下文、状态装配与组件编排。
- `[文件名].hooks.ts`：提取复杂 `useState`、`useEffect`、异步请求与副作用编排。
- `[文件名].types.ts`：集中存放 `interface`、`type`、枚举与对外契约。
- `_components/`：同级目录下拆分复杂子模块（如表格单元格、对话框内容、分区面板）。
- `hooks/`：当业务域复杂度持续增长时，按 Feature 建立目录化 hooks，避免单 hooks 文件再次膨胀。
- 后端服务类：按业务域拆分到独立文件（如 `permission-service.ts`、`notification-service.ts`、`data-service.ts`），避免一个 `service.ts` 处理 3+ 域。

## 5. 主页面职责（原子化）

- 主页面文件仅保留页面骨架、布局与组件装配。
- 页面内禁止保留已抽离到 hooks/components 的重复实现。
- 业务逻辑应从"页面内联"迁移到 hooks 或子组件，避免上帝组件。

## 6. 执行要求

- 修改前先评估目标文件行数与改动后体量；若有超限或域混杂风险，先拆分再实现。
- 大文件重构任务必须先输出《拆分清单（File Manifest）》，待用户确认架构后分步骤实施。
- 对超过 `300` 行的现有文件，禁止全量重绘式改写；必须采用"增量注入"或"模块重构"模式，控制单次改动范围。
- 拆分应优先按业务块聚合（筛选区、列表区、详情区、弹窗区、权限、数据、通知），避免无意义碎片化拆分。
- **每次改动自检清单**：
  - [ ] 每个函数是否只做一件事（<= 50 行）？
  - [ ] 文件是否混杂了 3 个以上的子业务域？
  - [ ] 是否存在为凑行数而压缩变量/取消换行的情况？
  - [ ] 复杂的逻辑闭环是否保留了中文结构化注释？
  - [ ] 是否已做繁体字扫描并清理？
