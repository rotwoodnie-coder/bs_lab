---
description: 对话框可见文案强制中文
alwaysApply: true
---

# 对话框文案中文化（全局约束）

本项目内所有“对话框类窗口”对用户可见的文案必须使用中文输出，禁止出现英文 UI 文案（除专有名词、缩写、协议名、代码标识外）。

## 适用范围

- `Dialog` / `AlertDialog`
- `Drawer` / `Sheet`
- `Modal`（含业务自定义弹层）
- 以上容器中的标题、副标题、按钮、占位符、提示文案、空态文案、错误提示、确认文案

## 硬性规则

1. 对话框内所有用户可见字符串默认使用中文。
2. 禁止英文按钮文案（如 `Save`、`Cancel`、`Close`、`Confirm`），统一改为中文（如“保存”“取消”“关闭”“确认”）。
3. 禁止英文占位符与空态文案（如 `Search...`、`No data`），统一改为中文。
4. `Toast` / `message` / `error` 若由对话框行为触发，文案必须为中文。
5. 若文案来自 i18n key，默认语言包（zh-CN）必须完整，不允许对话框出现英文回退。
6. 仅以下情况允许保留英文：技术缩写（API/ID/URL）、品牌名、课程标准中明确要求的术语；其余需提供中文释义或上下文。
7. 所有可见中文文案必须使用简体中文，禁止出现繁体字；发现繁体需在提交前统一转简体。

## 建议用词统一

- Save -> 保存
- Cancel -> 取消
- Close -> 关闭
- Confirm -> 确认
- Delete -> 删除
- Edit -> 编辑
- Search -> 搜索
- Loading -> 加载中
- No data -> 暂无数据

## 提交前检查清单

- 是否扫描本次改动中所有 `Dialog/Drawer/Sheet/Modal` 的可见文案？
- 是否存在英文按钮、占位符、空态、错误提示？
- 与弹窗交互相关的 `Toast` 是否为中文？
- 是否避免中英文混排导致表达不一致？

## 提交前自动扫描繁体字（强制）

凡涉及对话框文案改动，提交前必须执行一次繁体字扫描。

### 扫描方式

- 使用 `rg` 扫描本次改动文件或目录中的常见繁体字。
- 命中后必须先改为简体，再提交。

### 常见繁体字黑名单（基础集）

`請 實 說 點 擊 關 閉 開 啟 資 料 圖 示 範 圍 錄 入 步 驟 選 擇 頁 麼 為 與 會 後 臺 審 核`

### 推荐扫描命令（示例）

```bash
rg "請|實|說|點|擊|關|閉|開|啟|資|料|圖|示|範|圍|錄|入|步|驟|選|擇|頁|麼|為|與|會|後|臺|審|核" frontend packages/ui
```

### 通过标准

- 扫描结果为空，或命中项已全部改为简体。
- 若命中为专有名词/引用原文，需在变更说明中注明保留原因。
