---
description: POST/PUT 请求体字段名须与目标表 DDL 列名及约定 JSON 子键一致（snake_case）
alwaysApply: true
---

# POST/PUT Payload 与数据库列名对齐

## 硬性要求

1. **凡写入 MySQL 表某一列的请求体顶层 key**，必须与对应 `CREATE TABLE` 中的**列标识符完全一致**（含 `snake_case`，与 `database/migrations/bs_exp_data.sql` 或当前迁移中的 DDL 一致）。
2. **值写入 JSON 列（如 `comments`）时**：请求体中应使用与**持久化 JSON 对象**相同的 key 名；本仓库约定侧车子对象为 `_ref`（教材书目元数据）、`_ch`（章节扩展字段），其**内部字段**一律使用 **snake_case**（如 `subject_id`、`cover_registry_id`），与列名风格一致。
3. **禁止**在发往自研 API 的 JSON 里用 camelCase 表示「对应表列」的语义（例如不要用 `subjectId` 表示 `data_school_subject.subject_id` 或 `comments._ref.subject_id`）。
4. **前端 UI 层**仍可使用 camelCase 状态；**在发起 `fetch` 前**（通常在 `page.api.ts` 或同层 BFF）必须映射为上述规范，使**实际 HTTP body**满足本条。

## 审计方式

新增或修改 POST/PUT 时：打开目标表的 `CREATE TABLE`，逐项核对 body 与 JSON 子结构 key；路由层类型（`as { ... }`）应与 DDL 同源注释或从 schema 文件拷贝字段名。

## 例外

仅当对接**第三方固定 camelCase 协议**且无法改对方合同时，在规则或 PR 中显式注明例外范围；本仓库内首写 API 不适用例外。
