---
description: bsv0 目录仅作 UI 交互参考（只读禁用）
globs: frontend/src/**/*.{ts,tsx}
---

# bsv0 目录：仅 UI 交互参考（强制）

`bsv0/**` 目录在本项目中 **仅用于 UI/交互参考**，必须视为“只读参考库”。

## 禁止事项（硬性）

- **禁止修改** `bsv0/**` 下的任何文件（包括重构、格式化、修复 lint、补类型等）。
- **禁止在项目代码中直接使用/引用** `bsv0/**` 下的任何实现（例如 import 组件、复制整段实现到业务中、把它作为可运行源码接入等）。
- **禁止把 `bsv0/**` 当作真实源码目录排查/修复问题**；只允许用来对照 UI 交互与视觉效果。

## 允许事项（边界）

- 允许从 `bsv0/**` **提取设计意图**：布局结构、信息层级、交互手感、组件组合方式、命名方式与文案风格。
- 实现必须落在 `bs_lab` 的真实业务目录与标准库约束下（例如使用 `@bs-lab/ui` 及项目既有布局容器约定）。

