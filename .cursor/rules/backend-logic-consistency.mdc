---
description: 后端逻辑一致性（事务原子性、错误码规范、写操作审计）
globs: backend/src/**/*.{ts,tsx}
---

# 后端逻辑一致性规则（Hard Constraint）

## 1. 事务原子性

### 1.1 重算/副作用类函数必须包裹事务

涉及以下操作的函数**必须**在 Transaction 中执行：

- 跨表状态重算（如 `recalculateSubjectTags`、权限重算、学分累计）
- 写操作序列（先检查→再写入→再更新关联表）
- 先删后插（如批量更新角色绑定、组织挂载）

```typescript
// ✅ 正确
const pool = getMysqlPool();
const conn = await pool.getConnection();
try {
  await conn.beginTransaction();
  // ... 多步写入 ...
  await conn.commit();
} catch (err) {
  await conn.rollback();
  throw err;
} finally {
  conn.release();
}
```

### 1.2 严禁"部分写入"

禁止在没有事务保护的情况下对多个表连续执行 INSERT/UPDATE，避免中间失败导致数据不一致。**一条 SQL 不需要事务，两条及以上的写操作必须事务。**

---

## 2. 错误码规范

### 2.1 禁止裸 `throw new Error("xxx")`

业务逻辑中禁止直接 `throw new Error("中文消息")` 或 `throw new Error("错误码")`。必须使用**统一业务错误码枚举**或**自定义 Error 类**。

### 2.2 错误码定义模式

为每个 Service/Repository 定义专属错误码类型和错误类：

```typescript
// backend/src/services/XxxService.ts
export type XxxServiceErrorCode =
  | "XXX_NOT_FOUND"
  | "XXX_DUPLICATED"
  | "XXX_INVALID_STATE"
  | "PERMISSION_DENIED"
  | "INTERNAL_ERROR";

export class XxxServiceError extends Error {
  code: XxxServiceErrorCode;
  constructor(code: XxxServiceErrorCode, message: string) {
    super(message);
    this.code = code;
  }
}
```

错误码使用 **UPPER_SNAKE_CASE**，业务前缀（如 `CLASS_`、`EXP_`、`HOMEWORK_`）+ 具体语义后缀。

### 2.3 路由层错误处理模式

每个 route handler 的 catch 块应分层处理：

```typescript
try {
  // ... 业务逻辑 ...
} catch (err) {
  if (err instanceof z.ZodError) {
    return fail(`参数校验失败：${err.errors[0]?.message ?? "未知字段"}`, 400);
  }
  if (err instanceof XxxServiceError) {
    // 自定义错误码映射到 HTTP status + 中文提示
    const codeMap: Record<XxxServiceErrorCode, [number, string]> = {
      XXX_NOT_FOUND: [404, "xxx 不存在"],
      XXX_DUPLICATED: [409, "xxx 已存在"],
      XXX_INVALID_STATE: [400, "xxx 状态不允许此操作"],
      PERMISSION_DENIED: [403, "权限不足"],
      INTERNAL_ERROR: [500, "服务内部错误"],
    };
    const [status, message] = codeMap[err.code] ?? [500, "服务内部错误"];
    return fail(message, status);
  }
  console.error("[route-prefix]", err);
  return fail("服务内部错误", 500);
}
```

### 2.4 Zod 校验失败统一用中文

`parse` 产生的 ZodError 路由层必须拦截并返回中文提示（使用 `err.errors[0]?.message`），禁止将 Zod 英文默认错误直接返回前端。

---

## 3. 写操作审计

### 3.1 审计字段必填

所有 `INSERT`/`UPDATE` 操作必须填充 `create_user_id` / `update_user_id`（或等效的 actorId 参数），禁止留空或写死默认值。

### 3.2 Service 层接收 actorId

所有写操作 Service 函数的签名必须包含 `actorId`（或 `actorRoleId`）参数，用于传递给 Repository 层的审计字段。

### 3.3 日志记录

业务关键操作（创建/删除/状态变更/权限变更）必须记录操作日志，至少包含：操作人、操作时间、操作类型、操作对象 ID、变更前后摘要。

---

## 4. 参数校验

### 4.1 路由层统一使用 Zod schema

所有 POST/PUT/PATCH 请求体必须通过 `zod` schema 进行校验，禁止手动 `if/else` 逐字段校验。

### 4.2 导出类型同源

Zod schema 的 `infer` 类型必须作为 DTO 类型导出，避免在 Service/Repository 层重新声明输入类型。

---

## 5. HTTP 响应格式

所有 API 路由必须统一响应格式：

```typescript
// 成功
Response.json({ success: true, data: { ... } }, { status: 200 });

// 失败
Response.json({ success: false, data: null, error: { message: "中文错误描述" } }, { status: 4xx/5xx });
```

---

## 自检清单

- [ ] 涉及多步写入的操作是否包裹在事务中？
- [ ] 自定义错误类是否使用 UPPER_SNAKE_CASE 错误码？
- [ ] 路由层 catch 是否分层处理了 ZodError / ServiceError / 未知错误？
- [ ] 写操作是否传入了 actorId 且审计字段已填充？
- [ ] 请求体是否使用了 Zod schema 校验？
- [ ] 响应格式是否统一为 `{ success, data, error }`？
