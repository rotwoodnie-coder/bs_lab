---
description: 正式开发阶段禁止将原/旧数据库作为正式运行数据源，仅允许排查时只读使用
alwaysApply: true
---

# 原库只读排查、禁止作为正式数据源（硬性）

当前处于 **正式开发阶段** 时，**禁止**把「原有 / 旧版 / 迁移前」数据库（以下简称 **原库**）作为应用、联调、验收或任何「正式使用」场景下的数据依赖来源。

## 硬性规则

1. **禁止**在本地/联调/预发等开发环境中，将后端或前端的默认连接、主业务读写指向原库来完成日常开发与功能交付。
2. **禁止**把原库中的数据当作新架构（如 V2 schema、新迁移库）的权威真源来驱动页面与 API 的常态化开发。
3. **禁止**编写依赖原库的长期脚本、定时任务、或「顺手查一下原库」固化进业务代码路径。
4. 新功能、数据模型与迁移验证必须以 **当前项目约定的目标库 / 迁移后库** 为准；原库不得替代目标库承担「正式使用」角色。

## 允许的唯一例外：排查

仅在 **问题排查、数据对照、迁移差异分析、事故调查** 等明确需要时，可对原库进行 **只读** 访问（例如一次性 SQL、只读账号、临时导出对照），且应：

- 在说明中注明「原库、仅排查」；
- 排查结束后不得将结论依赖继续绑定在原库上；
- 不得将原库只读结论写回为「默认开发流程」的一部分。

## 自检清单

- 环境变量与连接串是否始终指向 **目标开发库**，而非原库。
- 是否有代码、脚本或文档仍引导他人「连原库做日常开发」。
- PR 中若出现原库连接或大范围原库查询，必须说明 **仅限排查** 且不得合并为常态化路径。
