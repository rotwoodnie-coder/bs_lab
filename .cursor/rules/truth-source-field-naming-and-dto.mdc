---
description: 以《01-数据库最终开发前设计文档》为字段命名真源；DTO/ViewModel 禁止 UI 层乱改名；载荷 snake_case / 前端 camelCase 严格映射
alwaysApply: true
---

# 真源、DTO 与命名映射（硬性）

## 与现有规则的关系

- **`db-schema-drives-ui.mdc`**：解决「谁迁就谁」与**已落地**库表、迁移、V2 API 契约对前端的驱动顺序。
- **本规则**：解决**字段词汇与结构**在「文档 ↔ 类型 ↔ UI」之间的一致性，以及 **snake_case / camelCase** 的不可含糊映射。

二者同时生效。若 **`docs/iam/01-数据库最终开发前设计文档.md`**（下称 **《01 文档》**）与当前 `database/migrations/*.sql` / `bs_exp_data.sql` 列名不一致，**不得私自拍板**：须走产品/技术评审后统一改文档或改库，再同步全栈。

---

## 原则 01：数据库设计文档是唯一命名真源

1. **所有**前端组件、API 契约（含 zod/OpenAPI 等）、TypeScript 中与**持久化实体**相关的**语义字段名**，必须与 **《01 文档》** 中对应表、字段的命名与拼写**严格对齐**（含历史拼写纠错结论，如文档已统一为 `video` 则禁止 `vedio`）。
2. **禁止**为「省事」使用缩写或口语化别名替代文档全名（例如文档为 `pic_url` 时，禁止在类型或 API 层长期用 `pic` 指同一列）。
3. **《01 文档》未定义**的字段、表或枚举：**必须先向用户/负责人确认**后再命名；**禁止**凭旧代码、Tab 补全或「常见习惯」私自造名。

---

## 原则 02：DTO 与 ViewModel 强一致（禁止 UI 层解构改名）

1. **后端 API 返回体（DTO）**中的键名，在语义上应与**库侧列名**一致（传输层为 **snake_case**，见原则 03）。
2. **禁止**在页面/组件里对 DTO 做「解构时顺便改英文名」**作为业务字段别名**（例如 `const { pic_url: pic } = row` 后全页用 `pic` 表示同一列）。业务含义在 UI 层应仍能通过类型追溯到**文档列名**对应的 camelCase 形态。
3. 前端 `interface` / `type` 与表结构在**字段集合与语义**上应 **1:1 映射**（允许缺失只读列在 UI 不展示，但不得在类型层用另一套「平行字段」冒充同一实体）。
4. **唯一例外 — 转换层（Transform / Adapter）**：仅在此层做**不改变语义键**的格式化，例如时间戳转 ISO 字符串、`y/n` 转 boolean；**转换后对象的主键名字段**仍须与 **camelCase 列语义**一致（如 `createdAt` 对应 `created_at`，禁止改成 `time` 等模糊名）。

---

## 原则 03：对抗旧代码惯性；snake_case 与 camelCase

1. **重构当前文件**时，若发现字段名与 **《01 文档》** 或当前目标库契约冲突，**优先纠错**（改代码与类型对齐真源），而不是保留错误拼写以兼容旧 Tab 习惯。
2. **禁止**依赖编辑器补全去延续**已知错误**或与文档不一致的字段名；合并前应对改动区做目视与类型检查。
3. **载荷与存储侧**（数据库列、SQL、JSON API 请求/响应体、日志中的持久化字段）：**一律 snake_case**。
4. **前端 TypeScript 与组件内部变量**（表示同一持久化字段时）：**一律 camelCase**。
5. **映射规则**（严格一对一）：`user_id` ↔ `userId`，`pic_url` ↔ `picUrl`。  
   **禁止**出现 `user_ID`、`userid`、`picURL`（除专有名词缩写且文档明确约定外）等与上述规则不一致的形态。

---

## 自检（涉及实体字段或 API 类型时）

- 每个持久化字段能否在 **《01 文档》** 中找到同名依据；找不到的是否已走确认流程。
- UI 是否仅在 **adapter/transform** 做格式化，而**未**在页面层引入文档外的「简称主字段」。
- API 与 TS 是否分别为 snake_case / camelCase，且映射无歧义。
