---
description: 动手前必须审计：列出逻辑路径 + 涉及文件清单，等用户 Confirm 再写代码
alwaysApply: true
---

# 执行前审计（Pre-Coding Audit）

## 强制流程

**在开始任何代码改动之前**，必须先完成以下两步，然后等待用户明确确认：

1. **逻辑路径描述**：用 1-3 句话说明实现思路（数据流、状态管理、接口调用方向）。
2. **涉及文件清单**：列出所有将被新增或修改的文件，标注操作类型（新增 / 修改 / 删除）。

```
逻辑路径：xxx → xxx → xxx
涉及文件：
  - [修改] backend/src/http/routes/v2-xxx.ts
  - [新增] frontend/src/app/.../page.hooks.ts
  - [修改] frontend/src/lib/v2/v2-xxx-api.ts
```

3. 用户回复确认（如"确认"、"开始"、"好的"）后，方可开始写代码。

## 豁免场景

以下情况可跳过审计直接执行：

- 单文件、单函数的**纯 Bug 修复**（不涉及新功能或跨文件改动）。
- 用户明确说"直接做"、"不需要审计"等免审指令。

## 目的

防止方向偏差、无效劳动和用户预期不符的改动，确保每次大改动都在用户掌控范围内。
