自愈触发器：当你被要求“优化系统稳定性”或“修复已知故障”时，必须首先读取 `SELF_HEALING_LOG.md` 和 `sys_log` 表的最新记录。

修复协议：

轮次1（溯源）：通过报错的 `trace_id` 找到对应的代码行。

轮次2（推演）：判断修复该 Bug 是否会破坏现有的安全边界（如：是否会导致删除锁失效）。

轮次3（补丁）：给出修复代码，并更新 `CURRENT_SYSTEM_STATE.md` 中的“已修复”列表。
